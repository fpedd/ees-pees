name: webots

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: make
      run: echo This is just dummy test # make clean all
      shell: bash
      working-directory: webots/controllers/internal
